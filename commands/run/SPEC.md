---
description: Run the app.
---
If the WORKING_DIR/MANIFEST.md or WORKING_DIR/build/ are missing, output:
  No app available.
Otherwise:
  Run the app in WORKING_DIR/build using the instructions in WORKING_DIR/README.md (if available) and WORKING_DIR/MANIFEST.md.
