---
description: Check that prerequisites are setup
---
If the WORKING_DIR is missing a MANIFEST.md, output:

MANIFEST.md is missing.

Otherwise, read the manifest and check that we have all the dependencies available. If not, list the missing dependencies.
