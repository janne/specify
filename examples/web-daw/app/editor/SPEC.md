Replaces the track area while opened.

A grid that covers the whole area from left to right, also vertically scrollable to allow for more pitches. An "Extend" button on the far right end of the grid adds four beats (one bar). Vertical rows defines different pitches. Columns defines time, measured in beats. Four beats in a bar.

There is also a "Delete region button"
