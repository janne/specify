#!/bin/sh

specify_dir="$(dirname "$0")/../app"
spec_file="$specify_dir/SPEC.md"
command="$1"
shift 2>/dev/null || true
arguments="$*"
spec_template="$(cat "$spec_file")"
prompt="$(printf "$spec_template" "$specify_dir" "$(pwd)" "$command" "$arguments")"
codex exec --skip-git-repo-check --sandbox workspace-write "$prompt"
echo
